apiVersion: v1
kind: Service
metadata:
  name: ecommerce-app-service
spec:
  type: {{ .Values.service.type }}
  selector:
    app: ecommerce-app
  ports:
    - protocol: TCP
      port: {{ .Values.service.port }}
      targetPort: 80
